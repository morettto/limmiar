---
import BaseLayout from '../../layouts/BaseLayout.astro'
import { content } from '../../lib/content'

const { title, body } = content['en-US']
---

<BaseLayout locale="en-US" title={title}>
  <p>{body}</p>
</BaseLayout>
