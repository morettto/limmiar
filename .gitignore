node_modules/
dist/
.wrangler/
playwright-report/
test-results/
.playwright-mcp/
*.log
.env
.env.*
!.env.example

# Serena MCP local project state (cache, memories, machine-local config)
.serena/

# .NET build output (apps/api)
bin/
obj/
TestResults/
*.user

# Artefacto de diff da cadeia de review (efémero, 1.3.0)
.harness/diff/

# Artefactos de ASR (glue WASM + .data com os pesos) usados só em dev local
# por apps/app/src/session/nemotron-loader.ts; produção serve de object
# store externo (VITE_ASR_MODEL_BASE_URL) -- ver S05-02 fatia 6.
apps/app/public/models/

# Relatório JSON do vitest (`--reporter=json --outputFile`), escrito por quem
# corre a suíte à mão; nunca é entrada de nenhum portão.
apps/app/coverage.json
